language: java
jdk: oraclejdk8
sudo: reqired # faster builds

before_install:
  - wget https://dl.google.com/linux/direct/google-chrome-stable_current_i386.deb
  - sudo apt-get update -qq
  - sudo dpkg -i ./google-chrome*.deb
  - sudo apt-get -f install

script: "mvn test"

